# CLAUDE.md

とん速（松のや専門メディア）のネイティブアプリ。**手順と環境は [README.md](README.md) にある**。
ここには**判断の拠りどころ**だけを書く。

## 作りは gyumesy-frontend-app、見た目は tonsoku-frontend-web

**アプリとしての作り**（配信データの取得・キャッシュ・ルーティング・シェル・通知・計測・
リリース手順）は **`gyumesy-frontend-app`** を写す。あちらは先にアプリ化してストア公開まで
済んでおり、**レビューも審査も通っている。写せば同じ指摘は出ない。**

**見た目**（配色・書体・文言・画面の構成）は **`tonsoku-frontend-web`** に揃える。
gyumesy の色（ピンク）・書体（Noto 一択）・文言（ギュメシー）をそのまま持ち込まない。

- **何を作る時も、先に両方を見る。** 記憶や推測で組まない。**必ず最新の `main` を見る**
  （`git fetch` してから `git show origin/main:<path>`。作業ツリーは動かさない）
- **写す時はコメントごと写す。** 踏んだ問題の説明が長く書いてある。持ってこなかった時は
  なぜ持ってこなかったのかをこちらに書く
- 違えるのは**違える理由を書ける時だけ**。理由はその場のコメントに書く

### 写したら必ず通す変換

web の CLAUDE.md「写したら必ず通す4つの変換」と同じ根で、**型検査もテストも通り、
画面を開くまで気づけない**種類のずれ:

| 変換するもの | 写したままだと |
|---|---|
| **色トークン**（gyumesy の `pink` / `pinkStrong` / `secondary`） | とん速に無い色で描かれる。`lib/core/theme/app_colors.dart` の名前に置き換える |
| **文言**（ギュメシー・松屋） | 他ブランドの名前が画面に出る。文言は web の `src/i18n/messages/` から写す |
| **配信の形**（`index.md` / 導出した `-sm` / 非 null 前提） | とん速は記事を JSON で出し、null を出す鍵がある（README の「配信データ」） |
| **ドメイン**（`gyumesy.com`） | `lib/core/config/app_config.dart` を通す |

## 松のやと松屋を混ぜない

**web の CLAUDE.md の同名の節に従う**（松屋の意匠を見た目に持ち込まない・非公式のメディア・
`松乃家` は中国語だけ・英語は `Matsunoya`）。**アプリは gyumesy（松屋）を写すので、
web より持ち込む機会が多い。**

**Bundle ID が `com.gyumesy.tonsoku` なのは運営者の名前空間であって、意匠ではない**
（ユーザーの判断）。画面やストアの掲載情報に gyumesy の名前を出さない。

## 画面に出す文言

**web の CLAUDE.md「画面に出す文言」に従う。** 書くのは状態だけ。理由・予定・こちらの落ち度・
内部の呼び名は書かない。**新しく作る前に web のカタログから探す。**

## 配色

**定義は `lib/core/theme/app_colors.dart` の中だけ。** 値と実測コントラスト比の典拠は
web の `src/assets/styles/main.css`。**web の値を変えたらここも直す。**

**塗りの `primary` を地の上に置かない ―― 文字でもアイコンでも罫線でも。** 地の上の赤は
`primaryText`。ダークでは基準に届かず、**ライトでは同じ使い方で足りてしまうので、
ライトだけ見ていると気づけない**（web がグローバルナビの現在地で踏んでいる）。

## 下タブ

**ホーム / マップ / クーポン / メニュー**（ユーザーの指定）。web の下タブ
（ホーム / カレンダー / クーポン / ランキング / メニュー）とは違う。

- **あぶれる導線（カレンダー・ランキング・通知設定・とん速とは・法務）はメニューに詰める**
- **メニューはタブへ移らずシートを開く**（web・gyumesy と同じ）
- **マップはアプリにしか無い面**（店舗限定の取扱店を地図で見せる。Issue #8。web の
  `/map/` はアプリへ誘導する LP で、リンクはアプリのマップを開く。`docs/deep-links.md`）。**機能は
  `gyumeshi-rader-app` に倣い、見た目は web に揃える**（レーダー風の暗地・ネオンを
  持ち込まない）。背景地図は同梱（README の「マップの背景地図」）
- カレンダー・ランキングなどメニュー内の面には、web と同じく「配信が無い面は出さない」を当てる

## 広告

**とん速は広告を必ず入れる**（gyumesy には無い）。AdMob を使う。

- **置き場は下タブの上のアンカーバナー（全タブ）と、記事の本文の後の 1 枠だけ**
  （web の `CoAdSlot` の `articleBottom` と同じ位置。web の記事 3 枠は写さない。
  web 側と相談したうえでのユーザーの判断）
- **マップのリワード動画は「店舗限定の表示の開放」**（見終えたら 6 時間。ユーザーの決定）。
  閉じている間の案内は品のチップの場所に置く（`MapUnlockNotice`）
- **本番の ID が空の枠は何も出さない**。全部空なら SDK・同意・ATT に一切触れない
  （web の `adsenseClientId` と同じ。`lib/core/config/ad_config.dart`）
- **手元・テストで本番の広告を出さない**（無効なインプレッションになる。web の `config.ts`
  の `adsenseClientId` と同じ扱い）。release 以外は Google のテスト用 ID、テストは
  広告の読み込みごと偽物（`adGatewayProvider`）

## 文書の置き場

**入口は `README.md`（手順・環境）と この `CLAUDE.md`（判断の拠りどころ）の2つ。**
それ以外の説明は `docs/` に置き、**必ずどちらかから参照する**。

## どのリポジトリを見るか

どれも `github.com/yamada-gyumesy/` にあり、手元は `~/project/` に並んでいる。

| リポジトリ | こういう時に見る |
|---|---|
| `gyumesy-frontend-app` | **アプリの作り全般。まずここ**（`lib/` `docs/` `fastlane/` `store/`） |
| `tonsoku-frontend-web` | **見た目・文言・画面の構成**。配色の典拠（`main.css`） |
| `tonsoku-backend-batch` | **配信データの形**（`schema/`・`docs/app-feed.md`） |
| `tonsoku-infra-terraform` | Firebase（アプリの登録）・CDN・ドメイン |
| `gyumeshi-rader-app` | **マップ**（`app/*.json` は牛めしレーダーと同じ形で出ている） |

配信データの形の意図や、これから変わる予定は tonsoku-backend-batch のセッションに聞く。推測で書かない。
